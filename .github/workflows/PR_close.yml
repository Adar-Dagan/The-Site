name: Deploy web app to Azure Static Web Apps stabilization environment

on:
  push:
    branches:
      - main

jobs:
  deploy-to-stabilize:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      deployment_environment: "stabilization"
      source_workflow: "PR.yml"